---
title: "Feather"
sidebarTitle: "Introduction"
description: "Build full-stack Python apps with Flask, Tailwind CSS and HTMX, without assembling authentication, admin and deployment yourself."
icon: "feather"
mode: "wide"
---

Feather is a full-stack web framework. **Flask** handles the backend, **Tailwind CSS**
the styling, **HTMX** the interactions that need the server, and **vanilla JavaScript**
the handful that need the browser.

It works with any AI coding assistant, and it's tuned for
[Claude Code](https://claude.ai/code). Every project ships a `CLAUDE.md` and an identical
`AGENTS.md` setting out the conventions, and `feather check` enforces the ones that can be
enforced. That combination is what lets an assistant check its own work instead of
guessing.

<CardGroup cols={2}>
  <Card title="Install Feather" icon="download" href="/installation">
    One `pip install`, then `feather new` from any directory.
  </Card>
  <Card title="Build your first app" icon="rocket" href="/quickstart">
    Scaffold, migrate and run a working app in about five minutes.
  </Card>
  <Card title="Why Feather exists" icon="lightbulb" href="/why-feather">
    The gap between Django and the SPA world, and how Feather fills it.
  </Card>
  <Card title="Deploy it" icon="ship" href="/deployment/overview">
    Docker and Caddy on a single VPS, in eight files.
  </Card>
</CardGroup>

## What's included

All of this is optional. Turn a feature on when you create the project, or add it later.

| Feature | Options |
| --- | --- |
| **Authentication** | Google OAuth with session management, approval workflow |
| **User management** | Admin panel for approvals, roles, suspension |
| **Multi-tenancy** | Domain-based or individual tenants (B2B and B2C) |
| **Background jobs** | Thread pool with concurrency control, or RQ on Redis |
| **Caching** | Memory or Redis |
| **File storage** | Local filesystem or Google Cloud Storage |
| **Email** | Resend for transactional email |
| **Dark mode** | Cookie-persisted toggle on every page, including admin |
| **Security headers** | CSP, HSTS, X-Frame-Options, Referrer-Policy in production |
| **Rate limiting** | In-memory, or Redis for distributed |
| **Events** | Pub/sub with sync and async listeners |
| **Error logging** | Database-backed, tenant-scoped |
| **Health checks** | `/health`, `/health/live`, `/health/ready` |
| **Request tracking** | Unique request IDs, JSON logging |

## The three-layer frontend

Start with components for anything static. Reach for HTMX when you need server data
without a page reload. Use islands only when state has to live in the browser. Most
features never get past the first two.

<CardGroup cols={3}>
  <Card title="Components" icon="box" href="/ui/components">
    Server-rendered Jinja2 macros. No JavaScript, no hydration, just HTML and CSS.
  </Card>
  <Card title="HTMX" icon="arrow-left-right" href="/ui/htmx">
    Server interactions without page reloads. Forms, search, pagination, like buttons.
  </Card>
  <Card title="Islands" icon="component" href="/ui/islands">
    Small JavaScript components for UI that needs client-side state.
  </Card>
</CardGroup>

<Note>
  New here? Read [Why Feather](/why-feather) for the reasoning behind the stack, then go
  to the [Quickstart](/quickstart).
</Note>
