---
title: "feather components"
description: "Every macro's real signature, read from the macros themselves."
icon: "box"
---

`feather components` reads macro arguments from the macros themselves, so it stays
accurate as they change.

```bash
feather components                                  # signatures and import lines
feather components --json                           # for tooling
feather components --markdown -o docs/components.md
```

## Your components too

Components your app defines under `templates/components/` are listed alongside the
framework's. One sharing a filename with a framework component is marked as overriding
it, which is how the template loader resolves it at runtime.

<Tip>
  Point an AI assistant at this command before it writes a template. The
  [component reference](/ui/components) covers the framework macros, but this covers
  yours as well.
</Tip>

## In your own docs

The `--markdown` flag writes a catalogue file you can commit, so a reviewer reading a pull
request sees the same signatures without running anything.

```bash
feather components --markdown -o docs/components.md
```
