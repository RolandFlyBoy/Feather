
### 6. Always Protect Routes
- Use `@auth_required` on all routes that need authentication
- Use `@admin_required` for admin-only routes
- `@login_only` is only for the pending and suspended status pages: it lets
  inactive accounts through, so anywhere else a suspended user keeps working
  (`feather check` warns: `login-only-action`)
