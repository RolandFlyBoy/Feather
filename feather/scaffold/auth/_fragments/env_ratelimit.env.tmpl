
# Rate limiting (Flask-Limiter)
# Where the counters live. A Redis URL makes one limit hold across every
# gunicorn worker and container; empty means per-process memory (dev only).
RATELIMIT_STORAGE_URI=
