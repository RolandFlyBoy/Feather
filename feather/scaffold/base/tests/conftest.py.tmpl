"""Pytest configuration for Feather apps.

The app under test is built here from TestingConfig rather than imported from
app.py. The app in app.py is configured for development, and a suite that
adopted those settings would be running against your own environment.
"""

import pytest

from feather import Feather


@pytest.fixture(scope="session")
def app():
    """Create application for testing."""
    return Feather("app", config_class="config.TestingConfig")


@pytest.fixture
def client(app):
    """Create test client."""
    return app.test_client()
