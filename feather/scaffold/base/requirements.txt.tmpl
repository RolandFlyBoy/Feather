# Project Dependencies
#
# The Feather framework brings Flask, SQLAlchemy, authlib and the rest of the
# core. The bracketed names are its optional extras: each pulls in what one
# feature needs, and only the features this app enabled are listed. Add
# another (pdf, gcs, postgres, redis, email, ratelimit, prod, test) if you turn
# a feature on later, or use feather-framework[all] for everything.
__FEATHER_REQUIREMENT_SPEC__
